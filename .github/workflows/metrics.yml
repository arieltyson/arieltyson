name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          
          # --- Layout & Style ---
          user: arieltyson
          template: classic
          config_timezone: America/Vancouver
          config_theme: tokyonight
          config_twocol: yes 
          
          # --- REMOVED THE TOP SECTIONS ---
          # Setting base to "" removes the Header, Activity, Community, etc.
          base: ""
          
          # --- Languages (Colorful Bar) ---
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          
          # --- 3D Calendar (The "Snake" Replacement) ---
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year
          
          # --- Coding Habits (FIXED ERROR) ---
          plugin_habits: yes
          plugin_habits_charts: yes
          plugin_habits_days: 14
          plugin_habits_facts: yes
          # Reduced from 200 -> 60 to prevent timeout/crashing
          plugin_habits_from: 60 
          
          # --- Achievements (FIXED ERROR) ---
          plugin_achievements: yes
          plugin_achievements_display: compact
          # We explicitly limit it to 8 to stop it from freezing
          plugin_achievements_limit: 8
